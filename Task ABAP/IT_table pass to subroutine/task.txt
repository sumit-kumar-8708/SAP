Write a program which reads data from employee table into an internal table. 
Then pass the internal table to a subroutine which should increase the salary of each employee by 1000. 
Print the results (employee details from the main program).
